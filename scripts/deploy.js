// We require the Hardhat Runtime Environment explicitly here. This is optional
// but useful for running the script in a standalone fashion through `node <script>`.
//
// You can also run a script with `npx hardhat run <script>`. If you do that, Hardhat
// will compile your contracts, add the Hardhat Runtime Environment's members to the
// global scope, and execute the script.
import hre from "hardhat";

// const initBankHoldings = hre.ethers.utils.parseEther("500");

// const Bank = await hre.ethers.getContractFactory("Bank");
// const bank = await Bank.deploy({ value: initBankHoldings });

// const Attacker = await hre.ethers.getContractFactory("Attacker");
// const attacker = await Attacker.deploy(bank.address);

// await bank.deployed();
// await attacker.deployed();

// console.log("Bank deployed to:", bank.address);
// console.log("Attacker deployed to:", attacker.address);

// console.log("Bank balance (Before Attack):", hre.ethers.utils.formatEther((await hre.ethers.provider.getBalance(bank.address)).toString()))
// console.log("Attacker balance (Before Attack):", hre.ethers.utils.formatEther((await hre.ethers.provider.getBalance(attacker.address)).toString()))

// await attacker.attack({ value: hre.ethers.utils.parseEther("1")});

// console.log("Bank balance (After Attack):", hre.ethers.utils.formatEther((await hre.ethers.provider.getBalance(bank.address)).toString()))
// console.log("Attacker balance (After Attack):", hre.ethers.utils.formatEther((await hre.ethers.provider.getBalance(attacker.address)).toString()))

